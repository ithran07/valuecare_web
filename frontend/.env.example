# Your Django API (the "backend" folder in this same repo/deployment)
VITE_API_BASE_URL=http://localhost:8000/api

# Optional — only needed to enable "Create an account" / order history.
# Same values as your POS Supabase project.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
